import React from 'react'
import {Alert} from 'react-bootstrap'

const NotFound = () => {
    return (
        <Alert  variant= "danger">
            Not Found
  </Alert>
    )
}

export default NotFound
